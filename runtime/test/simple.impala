extern array(int) -> [float];

fn main() -> [float] {
    let width   = 1024;
    let arr = array(width);
    let out = array(width);

    run_on_gpu(width, |tidx : int| -> void {
        arr[tidx] = out[tidx];
    });

    return out;
}
