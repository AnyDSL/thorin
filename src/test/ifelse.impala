def foo(x : int, y : int) -> int {
    a : int;
    if (x < 0)
        a = 23;
    else
        a = bar(x);

    b : int;
    if (y < 0)
        b = 23;
    else
        b = 42;
        
    return a + b;
}

def bar(x : int) -> int {
    return x + 1;
}
