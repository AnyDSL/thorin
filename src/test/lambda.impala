def f(a : int) -> int {
    i : int = 22;
    j : int = 22;
    b : bool = i == 23;

    g : pi(int) -> int = lambda(k : int) -> int { 
        def z(x : int) -> int { return x; }
        return i + j + a + z(k); 
    };

    if (a == 0)
        return g(23);
    else
        return g(g(42));
}

def g(i : int) -> int {
    return lambda(j : int) -> int { return j + 42 + i; }(23);
}
