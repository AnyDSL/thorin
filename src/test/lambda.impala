def f(a : int) -> int {
    i : int = 22;
    j : int = 22;
    b : bool = i == 23;

    g : pi(int) -> int = lambda(k : int) -> int { return i + j + a + k; };

    if (a == 0)
        return g(23);
    else
        return g(42);
}
