def f(a : int) -> pi(int) -> int {
    return lambda(i : int) -> int { 
        if (i == 3)
            return i + a; 
        else
            return i + i;
    };
}

def main(i : int) -> int {
    return f(i)(42);
}
