def my_while<L, O>(l : L,
                h : pi(L, O, pi(L, O, bool)), 
                b : pi(L, O, pi(L, O)), 
                n : pi(O)) 
{

/*
    #(int, bool) bla = head(m, 
    
    
    lambda(m : int, h : bool) {
        branch(h, 
            lambda() {
                body(m, lambda(m : int) {
                    while(m, head, body, next)
                })
            },
            lambda() {
                next(m)
            }
        )
    })
*/
}
